# 问题记录

# 1.3pass阶段的API请求老是超时是怎么回事？

## 根因

这不是单一网络超时，而是三个因素叠加：

1. 当前使用 SiliconFlow 的 `zai-org/GLM-5.2`，默认开启推理。
2. Pass 3 是请求最重的阶段：同时输入完整精修稿、来源包和大型工具 Schema，并要求输出课程、Quiz、练习、代码、参考答案及引用。
3. 原代码没有设置 `max_tokens`，超时后还会按 180 秒固定重试 5 次。

日志中的 `durationMs: 931495`，约等于 **15 分 31 秒**，正是五次长超时加退避等待的结果。

真实探针结果：

- 普通短请求：连续成功，耗时 2.4–3.5 秒，说明当前基础连接正常。
- 只要求返回 `OK` 且 `max_tokens=8`：模型仍使用 **110 个 reasoning tokens**。
- 增加 `enable_thinking:false`：reasoning tokens 降为 **0**，只输出 1 token。

SiliconFlow 官方接口支持 `max_tokens` 和 `enable_thinking` 控制。[SiliconFlow Chat Completions 文档](https://api-docs.siliconflow.cn/docs/api/chat-completions-post)

## 已修复

- Pass 3 限制最大输出为 12,000 tokens，并关闭 SiliconFlow 推理模式：[pipeline.ts (line 691)](Y:/MyAgentProject/src/agents/pipeline.ts:691)
- 超时重试从最多 5 次降至 2 次，避免同一重任务反复堆积：[openaiCompatible.ts (line 46)](Y:/MyAgentProject/src/providers/openaiCompatible.ts:46)
- 仅向 SiliconFlow 发送 `enable_thinking`，不会破坏其他兼容服务：[openaiCompatible.ts (line 32)](Y:/MyAgentProject/src/providers/openaiCompatible.ts:32)
- 增加通用请求选项：[types.ts (line 35)](Y:/MyAgentProject/src/providers/types.ts:35)
- 增加厂商适配回归测试：[openaiCompatibleProvider.test.ts (line 1)](Y:/MyAgentProject/tests/regression/openaiCompatibleProvider.test.ts:1)

## 验证

- 完整回归测试：39/39 通过
- TypeScript 类型检查通过
- 构建通过
- 没有执行完整 `start`，避免额外付费调用和直接发布学习产物

另外，日志中的 `fetch failed` 也曾同时出现在 Tavily 和诊断对话中，说明当时确实存在间歇性网络波动；但 Pass 3 持续超时的主要放大原因是**默认深度推理 + 超大结构化输出 + 五次长重试**。现在可以重新运行 `npm run dev start` 验证实际生成。

 ## 刚才的这些修改会对生成内容的质量产生影响吗？尤其是Pass 3 限制最大输出为 12,000 tokens?

会有潜在影响，但要分开看。**目前不能严谨地声称“质量完全不变”**，因为还没有做真实课程 A/B 对比。

## 12,000 Tokens 限制

`max_tokens=12000` 是**上限，不是目标长度**：

- 如果模型正常只需要 5,000 tokens，结果完全不受影响。
- 只有输出超过 12,000 tokens 时才会截断。
- 普通单元的 Draft 被要求控制在 1,500 中文词以内，再加 Quiz、练习和参考答案，通常有较大余量。
- 大型 Project、复杂代码或大量引用更可能接近上限。

因此：

- **普通单元风险较低**
- **Project 单元风险中等**
- 它主要影响生成成功率，而不是静默降低内容质量：工具调用被截断后通常无法通过结构化解析和质量门禁，产物不会发布。

不过当前 Provider 尚未显式检查 API 的 `finish_reason === "length"`。最好补上该检查，把“输出被截断”明确记录为质量失败，而不是只表现为解析错误。

## 关闭 Pass 3 推理

这一项比 12,000 tokens 更值得关注。

Pass 3 虽然叫“格式化与精修”，但实际上还负责生成：

- Quiz 与诊断性干扰项
- 编程练习和测试
- 参考解答
- 目标覆盖证据
- Citation claim
- Project 结构

所以它并非纯格式化任务。关闭推理可能：

- 提高速度和成功率
- 减少模型在简单结构化任务上的无效思考
- 但也可能降低复杂 Project、测试设计和参考解答的推理质量

Pass 1、Pass 2 仍使用默认推理，因此课程正文质量受影响较小；主要风险集中在**练习、测试和 Project 设计**。好在参考答案仍会实际运行，题目也有确定性质量门禁，明显错误不会静默发布。

## 更优方案

建议后续把 Pass 3 拆开：

```
Pass 3A：正文与结构组装
- 关闭推理
- 低温度
- 复用 refinedDraft
- 不重新生成整篇 lesson

Pass 3B：Quiz、Exercise、Reference Solution
- 开启有限推理
- 设置 reasoning budget
- 单独设置输出上限
- 运行参考答案与全部测试

Pass 3C：确定性发布验证
- 引用、目标覆盖、题目诊断度
- finish_reason 检查
- Schema 校验
```

这比当前“一次请求同时完成所有事情”更快、更稳定，也更能保护质量。

## 客观结论

- `12,000 tokens`：目前看较安全，但需要监控接近上限的请求。
- 关闭推理：普通课程风险较低，复杂 Project 存在质量风险。
- 当前回归测试只能证明程序正确，**不能证明教学质量不下降**。
- 最严谨的验证方式是对普通单元和 Project 各生成一组 A/B 样本，对比发布通过率、代码测试通过率、人工评分、延迟和 token。

在完成 A/B 前，我建议保留当前修复解决超时，但增加：`finish_reason` 检查、输出使用率指标，以及 Project 单元的有限推理模式。