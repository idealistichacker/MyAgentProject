# 问题记录

> 最后更新：2026-07-14
> 适用分支：`codex-course-generation-optimization`

本文记录课程生成链路中已经真实复现的问题、根因、修复和验收结果。结论优先来自本地代码、generation job、缓存产物和真实 `npm run dev start`，不把推测写成已验证事实。

## 1. Pass 3 API 请求长时间超时

### 现象

- Pass 1 通常完成，但 Pass 3 或随后的 structured repair 多次超时。
- 旧日志单次作业可持续约 15 分钟，且同一个重请求被反复重试。
- 用户最初使用 SiliconFlow 的推理模型，后来切换为 `deepseek-ai/DeepSeek-V4-Pro`。

### 根因

这不是单一网络故障，而是多个因素叠加：

1. Pass 3 同时消费完整草稿、来源包和大型工具 Schema，并生成 lesson、Quiz、练习、测试、参考解答、目标覆盖和引用，是链路中最重的调用。
2. 旧实现没有为不同阶段定义独立的 token、推理和重试策略；超时后可能重复执行完整大请求。
3. structured repair 曾重新发送并重写完整 artifact，实际负载不低于 Pass 3。
4. 确定性质量错误也会触发重型模型修复，造成“看起来像 API 一直超时”的放大效应。

### 已修复

- 在 `src/timeouts.ts` 集中定义 Draft、Critique、Pass 3、assessment repair、citation repair 等阶段策略。
- Pass 3 设置 `maxTokens: 12_000`、有限重试和提供方感知的 thinking 控制。
- 将 assessment 与 citation 问题从完整 artifact repair 拆为局部结构化修复。
- assessment repair 最多两轮，只传 Quiz、目标覆盖和精确错误，不重新发送完整 lesson。
- citation repair 最多两轮，只允许从当前来源 ID 和 lesson 逐字事实候选中选择。
- 同一请求的并发 miss 通过 single-flight 合并，避免重复付费调用。

### 关于 12,000-token 上限

`maxTokens: 12_000` 是输出上限，不是目标长度。只要模型在上限内完成工具调用，就不会因为设置上限而降低内容长度或质量；若输出被截断，结构化解析和质量门会失败，产物不会静默发布。

当前更值得关注的是上下文体积和职责耦合，而不是继续无上限放大输出。普通单元有充足余量；大型 Project 仍应监控 completion token 使用率，并最终把正文组装与评测生成拆成独立阶段。

## 2. Pass 3 成功后仍反复进入修复

### 现象

Pass 3 已打印“格式精修完成”，随后却因以下问题拒绝发布：

- `quiz.choice.distractor.tooSimilar`
- `quiz.objective.unassessed`
- citation claim 不在 lesson 中
- citation claim 缺少一个 primary 来源或两个独立 publisher 支持
- objective coverage evidence 不是 lesson 的逐字片段

### 根因

“模型返回成功”只代表 API 和工具调用完成，不代表 artifact 已通过发布质量门。原始修复路径又把不同类型的错误全部当作“重写整份课程”，导致上下文大、修复目标不明确，并可能修坏已经正确的字段。

### 已修复

- 动态工具 Schema 将 `objectiveIds` 限制为计划中的精确 objective 值。
- 干扰项检查报告具体冲突选项对，而不是只报告整道题。
- assessment repair 只合并 Quiz 与 objective coverage，保持 lesson、exercise、tests、citations 和 reference solution 不变。
- 最后一轮 assessment repair 可将持续冲突的选择题改为短答题，避免为了表面保留题型而生成伪差异干扰项。
- objective evidence 在合并前归一化为 lesson 中的逐字句子或代码行，并过滤无效 assessment ID。

这类修复不会放松质量门，而是缩小模型可修改的字段范围，提高修复成功率和结果稳定性。

## 3. 事实声明核验反复失败

### 现象

assessment repair 通过后，引用校验继续报告：

- 模型用中文改写事实，claim 无法在 lesson 中逐字找到。
- 同一事实只有一个非 primary 来源，达不到独立交叉支持要求。

### 根因

旧 citation repair 允许模型自由改写 claim，并且没有在 Schema 层约束来源 ID。提示词要求“逐字引用”不足以保证模型严格遵守。

### 已修复

- 从 lesson 确定性提取可引用事实句候选。
- citation repair 的 `claim` 和 `sourceId` 使用动态 enum，只能选择真实 lesson 句子和当前 SourcePack ID。
- 按 claim 分组检查支持关系：一个 primary 来源，或两个独立 publisher。
- 支持不足的声明不会伪造第二来源；若仍存在其他受支持声明，则确定性移除不确定声明，否则质量门继续失败并禁止发布。

## 4. Python 参考答案被误判失败

### 现象

引用和目标覆盖修复通过后，参考答案实跑出现：

```text
json.decoder.JSONDecodeError: Expecting ',' delimiter
```

错误发生在自动生成的 `test.py` 中，而不是参考答案业务逻辑中。

### 根因

Python runner 把 `JSON.stringify(testCases)` 直接插入单引号 Python 字符串，再调用 `json.loads(...)`。当用例包含嵌套 JSON、转义引号、换行或英文撇号时，JavaScript 与 Python 两层字符串转义发生冲突，测试文件本身无法解析。

此外，生成用例使用 `expected: "ValueError"` 表示预期异常，但 runner 原先把任何异常都判为失败。

### 已修复

- 测试数据先以 UTF-8 JSON 序列化，再 Base64 编码后嵌入 Python 测试脚本，彻底隔离两层字符串语法。
- Python runner 支持以 `*Error` / `*Exception` 名称表达预期异常，并比较真实异常类型。
- 新增包含嵌套 JSON、英文撇号和 `ValueError` 的回归测试。

对应实现：`src/runner/pythonRunner.ts`；对应测试：`tests/regression/referenceVerifier.test.ts`。

## 5. DeepSeek V4 Pro 真实验收

2026-07-14 使用 `deepseek-ai/DeepSeek-V4-Pro` 真实执行：

```powershell
npm run dev start
```

最终结果：

- `unit-1` 通过结构化解析、教学质量门、事实声明门和参考答案实跑。
- 课程与练习安全发布，generation job 状态为 `published`。
- 最终验收作业：`unit-1-1783997422846-298d508a.json`。
- 总耗时：`82,551 ms`。
- Provider 调用：`2` 次。
- Provider 重试：`0` 次。
- Token：prompt `9,255`，completion `4,242`，合计 `13,497`。
- publication 阶段：`56 ms`。

结论：切换模型后的最终失败不再是 API timeout。DeepSeek V4 Pro 单次复杂修复仍可能需要一分钟以上，但在当前阶段策略下没有发生超时或重试；此前连续失败的直接原因是质量门逐层暴露的局部 artifact 问题和 Python runner 序列化缺陷。

## 6. 验证结果

- `npm test`：50/50 通过。
- `npm run test:integration`：8/8 通过。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `git diff --check`：通过，仅有 Git 的 LF/CRLF 工作区提示。

## 7. 尚未解决的风险

1. 本地 runner 只有受限环境变量和超时，不是 OS 或容器级安全沙箱。
2. `pipeline.ts` 仍承担较多生成、验证和修复职责，应继续拆分深模块。
3. 质量校验目前按顺序 fail-fast，可能让一个真实作业经过多轮昂贵调用后才暴露后续错误；后续应聚合可并行发现的质量问题。
4. 真实性能基线只有少量课程和单一 Python 单元，尚不足以证明跨语言、Project 和 remediation 的 p95 改善。
5. API 密钥不应长期明文保存在项目配置或终端日志中；若曾被输出，应立即轮换并迁移到环境变量或安全凭据存储。

## 8. 后续排障准则

- 先区分 provider timeout、structured-output 错误、质量门失败和 runner 失败，不把它们统一归类为“模型超时”。
- 每次失败先读取 generation job 的 `stage`、`qualityReport` 和 provider metrics。
- 对确定性错误优先局部修复或纯代码归一化，不重新生成完整 artifact。
- 任何修复不得绕过发布质量门，也不得覆盖学习者已有 `solution.*`。

## 9. 新计划错误复用旧计划的同 ID 课件

### 现象

重新执行 `diagnose` 和 `plan` 得到 Rust Web 后端课程后，新计划的 `unit-1` 是“所有权初体验”，但执行 `start` 直接显示课件已发布，并打开了上一次 Python 课程生成的 `unit-1.md`；练习同时显示“未生成”。

### 根因

新旧计划都从 `unit-1` 开始编号。CLI 的 `start` 与 `generate-all` 只检查同 ID manifest 的状态是否为 `published` / `offline`，没有验证 manifest 的 `inputHash` 是否属于当前 learner profile、单元规格和 Project 上下文。因此旧计划的成功 manifest 被误认为新计划的当前产物。

### 已修复

- 把 generation input hash 计算提取为 orchestrator 的统一公开能力。
- 对 generated plan 只哈希稳定生成规格，排除发布后新增的 lesson、Quiz、exercise、sources 和 citations，保证同一产物发布后仍可复用。
- `start` 与 `generate-all` 共用 `isArtifactManifestReusable`；只有状态可复用且输入哈希完全匹配时才跳过生成。
- 同 ID 但标题、目标、画像或 Project 上下文变化时，旧 manifest 自动失效，不需要删除用户数据。
- 新增回归测试覆盖“发布后字段不改变输入身份”和“同 ID 新课程必须拒绝旧 manifest”。

本地真实探针中，旧 Python manifest hash 为 `1a3c6a...`，当前 Rust 单元 hash 为 `fb31baf...`，修复后判定 `reusable=false`。下一次 `start` 将进入 Rust 单元生成流程，而不会继续展示旧 Python 课件。

## 10. Rust 单元引用修复后仍无法发布

### 现象

Rust 所有权单元的 Pass 3 已完成，但先后出现：

- 初始 citation claim 直接复制了英文来源，而不是中文 lesson 中的逐字事实句，触发 `fact.claim.notInLesson`。
- 局部 citation repair 完成后，质量门才继续暴露 objective coverage evidence 与 lesson 的 Markdown 表记不一致。
- 上述质量问题修复后，真实流程进入参考解答验证，但本机返回 `spawn rustc ENOENT`。

### 根因

1. 原始 SourcePack 只有博客等二手来源，严格的“一项 primary 或两项独立来源”规则缺少稳定输入基础。
2. broad Tavily 搜索没有保证技术主题包含官方文档；引用修复即使格式正确，也可能没有足够可信的来源可选。
3. objective evidence 的逐字归一化只在 assessment repair 路径执行。若先发生 citation 错误，引用修复通过后才会暴露此前被 fail-fast 顺序遮蔽的证据格式问题。
4. 当前机器未安装 `rustc`。这不是 API、引用或模型质量错误，而是 Rust 参考解答真实编译所需的本地运行时缺失。

### 已修复

- SourcePack 缓存版本升级；Rust、Python、TypeScript/JavaScript、Node、Go、Java 技术主题在 broad 搜索缺少 primary 来源时，自动补充限定官方域名的 Tavily 搜索。
- 官方补充搜索失败时保留 broad 结果，不把一次补充请求故障扩大为整次检索失败。
- citation 候选排除 Markdown 标题、不完整的冒号引导句，并去除有序列表前缀，减少不可核验或无语义声明。
- objective coverage evidence 在所有生成产物进入质量门前统一归一化，不再只依赖 assessment repair；无法映射回 lesson 的证据仍会失败，不降低质量门槛。
- Rust runner 将 `ENOENT` 转换为明确的 `rustc` / `rustup.rs` 安装与重启终端指引，避免误判为模型生成失败。

### 真实验收结果

2026-07-14 使用相同 Rust 单元重跑 `npm run dev start`：

- SourcePack 命中包含 Rust 官方文档的缓存，检索包由原来的 2,059 字符提升到 4,862 字符。
- 初始英文 citation 仍被质量门拒绝；bounded citation repair 随后成功通过事实声明校验。
- objective coverage evidence 已通过统一归一化，没有再次出现“证据不在 lesson”错误。
- 流程最终仅因本机缺少 `rustc` 停在参考解答验证，产物未发布，符合“未通过真实测试不得发布”的约束。

要完成 Rust 课程端到端发布，需安装 Rust 工具链，在新终端确认 `rustc --version` 后重新执行 `npm run dev start`。现有 LLM 与搜索缓存会复用，不需要重新支付已缓存阶段的调用成本。

## 11. 内容质量快速预览模式

为了让提示词、来源策略、Pass 1～3 和质量规则可以快速迭代，`start` 新增：

```powershell
npm run dev start -- --content-only
```

该模式仍会执行来源可信度、结构化 Schema、正文与练习完整性、题目诊断度、事实引用、目标覆盖和 Project 综合规则，也会执行对应的 bounded repair；唯一跳过的是参考解答的本地编译与测试。

预览产物写入 `.fuckcolloge/previews/<unitId>/`，包括 `lesson.md`、`artifact.json`、starter code 和可选的 `PROJECT.md`。预览作业状态为 `preview`，不会更新正式学习计划、不会创建或覆盖发布 manifest，也不会创建或覆盖学习者 `solution.*`。

正式模式和预览模式使用隔离的最终产物缓存，避免预览结果在后续正式发布时绕过参考解答实跑；Pass 1、Pass 2 和 Pass 3 的阶段缓存仍可共享，以保持开发迭代速度。正式发布仍必须执行不带 `--content-only` 的 `npm run dev start` 并通过完整 runner 验证。

批量预览使用 `npm run dev -- generate-all --content-only`。它会把计划中的每个单元都加入预览调度队列，即使该单元已有正式 manifest；现有 `--concurrency`、`--stagger-ms`、`--requests-per-minute` 和熔断策略继续生效。离线种子单元同样只写预览目录，不会误进入 `offline` 正式发布路径。

## 12. 全量内容预览仅部分成功

### 现象

首次真实执行 `npm run dev -- generate-all --content-only` 时仅 3/8 单元成功，失败并非同一类问题：

- Tavily 偶发 `fetch failed`，单次网络抖动直接终止单元生成。
- 某些技术目标没有显式包含语言名，例如只写 `impl blocks`，导致官方 Rust 来源补充策略未被触发。
- choice 题的干扰项修复后仍然过度相似，重复请求模型不能保证收敛。
- objective evidence 错误被错误路由到只允许修改 assessment 的局部修复，模型无法补写缺失的 lesson 内容。
- 通用结构化修复只尝试一次；模型返回畸形 tool arguments 后没有携带失败反馈继续纠正。
- Windows 上临时文件替换偶发 `EPERM`，使已生成产物在原子落盘阶段失败。

### 根因

批量模式放大了单次生成中原本低概率发生的提供方网络故障、模型结构化输出偏差和 Windows 文件系统竞争。原有实现同时缺少三类收敛机制：检索输入没有可靠来源就绪门，修复路由没有区分“只能改题目”和“必须改正文”，确定性可修复错误仍完全依赖再次调用模型。

### 已修复

- Tavily 对网络错误、超时、408、429 和 5xx 执行最多三次短退避重试；失败的来源包不写入缓存。
- SourcePack 缓存版本升级，并要求 Tavily 缓存至少包含一个 primary 来源或两个独立 publisher。
- 技术查询先提取稳定英文关键词，再补充官方域名检索；Rust 识别同时覆盖 `impl blocks`、`Result<` 等不含语言名的目标描述。
- 当 Tavily 结果仍不满足来源门时，对 Rust、Tokio、Axum、SQLx 和 Redis 等已知主题直接读取对应官方文档页面，形成可核验 primary 来源。
- objective coverage 错误改为结构化质量错误，并路由到可修改完整 artifact 的修复路径；assessment 局部修复不再承担正文补写职责。
- 通用 artifact repair 改为最多三轮的有界循环，后续轮次携带解析或质量门反馈及上轮结构化参数。
- choice 干扰项在两轮局部修复后仍过度相似时，确定性转换为 short-answer 题并保留 rubric，避免无限重试且不绕过质量门。
- Windows 原子 rename 对 `EPERM`、`EACCES` 和 `EBUSY` 执行最多五次短退避重试；其他文件系统错误立即失败。

### 验收结果

- 回归测试覆盖 Tavily 瞬时网络恢复、缓存版本与来源门、objective 修复路由、choice 确定性收敛及 Windows rename 重试。
- 首轮修复后的真实批量重跑中，原先持续失败的 `unit-5` 已通过，证明 choice 确定性收敛路径生效。
- 修正 Rust 查询识别和官方页面兜底后，`unit-2` 的来源包由不可发布状态提升到 6,167 字符，并真实完成 `--content-only` 预览，通过完整内容质量门。
- `unit-3` 重新执行 Pass 1～3 后，初始 citation 仍被严格门拒绝，但有界局部修复成功收敛并生成隔离预览，未再出现畸形 tool arguments 导致整单元终止。
- DeepSeek V4 Pro 的单次 Pass 仍可能持续数分钟；这是提供方响应延迟，不应与检索失败、结构化质量错误或原子写入失败混为一谈。批量开发建议继续使用有限并发、stagger 和请求速率限制。

## 13. 严格质量门难以快速关闭，跨主题产物使用相同文件名

### 现象

- 每次生成都会执行完整教学质量门。开发者只想快速观察模型原始输出时，也可能因引用、目标覆盖或题目诊断规则失败而拿不到预览。
- 新旧课程计划都从 `unit-1`、`unit-2` 编号；正式讲义、练习和预览原先只使用 `unitId` 作为物理路径，切换主题后会覆盖旧主题文件。

### 根因

1. `providerConfigSchema` 没有质量门开关，`pipeline.ts` 在结构化解析后无条件调用严格教学质量校验。
2. 最终缓存键没有记录质量门模式，直接增加开关会有“宽松结果被严格流程复用”的风险。
3. 路径工具只接收 `unitId`，而 `unitId` 是课程内部稳定标识，不是跨课程唯一的产物标识。

### 已修复

- `.fuckcolloge/config.json` 新增 `qualityGateEnabled`，默认 `true`；`fc config` 会显示当前值。
- 关闭时只跳过严格内容质量规则，不跳过来源包、结构化 Schema 和正式模式参考解答运行。
- 严格/宽松模式进入不同最终缓存键；宽松正式产物标记为 `degraded`，重新开启质量门后不会被当作严格产物复用。
- generation job 与 artifact manifest 持久化质量门模式，失败重试必须沿用原作业模式。
- 正式讲义、练习和预览改用 `<内容主题>-<unitId>`；标题会规范化为 Windows 安全路径并限制长度。
- manifest 增加产物命名版本。旧 manifest 不会被错误复用，下一次生成会写入主题化路径；旧文件不主动删除。
- `submit`、现有产物定位和发布器统一使用主题化路径，避免“生成成功但提交找不到 solution”的半迁移问题。

### 行为边界

- 不同主题、相同 `unitId` 不再互相覆盖。
- 同一主题、同一 `unitId` 的再次生成仍更新同一组产物；学习者 `solution.*` 继续默认保护，只有显式 reset 才覆盖。
- 关闭质量门适合开发预览和模型调试，不代表内容已通过事实、教学或诊断质量验收。
