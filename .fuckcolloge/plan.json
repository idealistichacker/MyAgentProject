{
  "learnerProfile": {
    "target": "最终学习目标：**用 Rust 在 Linux 环境编写一个用户态模拟操作系统，实现包含内存管理、进程调度和简陋文件系统三大核心模块的完整玩具内核。**  \n\n接下来我会为你生成一条针对性的学习路径，准备好迎接秃头之旅吧～ 💖",
    "programmingLevel": "basic",
    "dsaLevel": "heard",
    "weeklyHours": "2-5",
    "totalWeeks": "5-8",
    "learningStyle": "project-first",
    "codePractice": "yes",
    "pace": "normal",
    "nearTermGoal": "无",
    "rawAnswers": {},
    "summary": "该学习者具备基础编程经验，但对数据结构与算法仅有初步了解。他计划在5-8周内每周投入2-5小时，采用项目驱动的学习方式，用Rust在Linux环境构建一个包含内存管理、进程调度和简易文件系统的用户态模拟操作系统。学习风格偏好从实际项目切入，在构建中逐步融入系统编程知识，适合以分阶段实现内核模块为主线进行引导。由于时间有限且基础较浅，需要设计紧凑且循序渐进的学习路径，确保在短期内完成可运行的操作系统原型。",
    "createdAt": "2026-06-11T08:32:49.081Z"
  },
  "units": [
    {
      "id": "rust-sys-foundations",
      "type": "unit",
      "title": "Rust 系统编程基础与 Linux 环境搭建",
      "description": "快速掌握 Rust 核心概念（所有权、借用、生命周期、智能指针）并配置 Linux 开发环境。重点学习 no_std 编程、unsafe Rust 基础以及如何在用户态模拟底层操作。参考 The Rust Book 第4-10章及 Philipp Oppermann 的 A Freestanding Rust Binary 教程。",
      "prerequisites": [],
      "objectives": [
        "能在 Linux 下用 cargo 创建并编译 Rust 项目",
        "理解所有权、借用、生命周期三大核心机制",
        "掌握 Box、Rc、Arc、RefCell 等智能指针的使用场景",
        "了解 unsafe Rust 的基本用法与 FFI 边界",
        "能编写不依赖标准库的 freestanding Rust 程序"
      ],
      "content": "# Rust 系统编程基础与 Linux 环境搭建\n\n## 一、Linux 开发环境与 Cargo 快速上手\n\n### 1.1 安装 Rust 工具链\n通过 `rustup` 一键安装：\n```bash\ncurl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh\n```\n安装后获得 `rustc`、`cargo` 和工具链管理器。验证：\n```bash\nrustc --version   # e.g. 1.84.0\ncargo --version\n```\n`rustup` 支持 stable/beta/nightly 三通道。编写 freestanding 程序时，若需自定义 `eh_personality` 等实验特性，可安装 nightly：\n```bash\nrustup toolchain install nightly\nrustup override set nightly   # 仅当前项目使用\n```\n但稳定版已支持 `#![no_std]` 和 `#[panic_handler]`，只需在 `Cargo.toml` 中设置 `panic = \"abort\"` 即可避免需要 `eh_personality`。\n\n### 1.2 Cargo 项目生命周期\n核心命令速查：\n\n| 命令 | 作用 |\n|------|------|\n| `cargo new <name>` | 创建项目（`Cargo.toml` + `src/main.rs`） |\n| `cargo build` | debug 编译，产物在 `target/debug/` |\n| `cargo build --release` | 优化编译，产物在 `target/release/` |\n| `cargo run` | 编译并运行 |\n| `cargo check` | 仅类型检查，极快，适合开发中验证 |\n| `cargo add <crate>` | 添加依赖（Rust ≥1.62 内置） |\n\n`Cargo.toml` 记录元数据与依赖，`Cargo.lock` 锁定精确版本。Cargo 自动解析依赖图并调用 `rustc`。\n\n---\n\n## 二、所有权、借用与生命周期 —— 内存安全三定律\n\n### 2.1 所有权（Ownership）\n三条铁律：\n1. 每个值有且仅有一个所有者。\n2. 所有者离开作用域，值被 `drop`。\n3. 所有权可转移（move），转移后原变量失效。\n\n```rust\nlet s1 = String::from(\"hello\");\nlet s2 = s1;          // s1 所有权移动至 s2\n// println!(\"{s1}\");  // 编译错误：s1 已失效\n```\n实现 `Copy` trait 的类型（如 `i32`、`bool`、`&T` 等）赋值时自动按位复制，不发生 move。`String`、`Vec` 等堆类型不实现 `Copy`，赋值即 move。\n\n**陷阱**：结构体部分移动（partial move）会导致整体不可用，除非仅移动 Copy 字段。例如：\n```rust\nlet p = (String::from(\"hi\"), 42);\nlet s = p.0;   // p.0 被移动\n// println!(\"{:?}\", p);  // 错误：p 整体部分移动后无效\n```\n\n### 2.2 借用（Borrowing）\n- `&T`：不可变借用，可同时存在多个。\n- `&mut T`：可变借用，同一时刻仅一个，且与不可变借用互斥。\n\n```rust\nlet mut v = vec![1,2,3];\nlet r1 = &v;\nlet r2 = &v;         // 允许多个不可变借用\n// let rm = &mut v;  // 错误：存在不可变借用\nprintln!(\"{r1} {r2}\");\nlet rm = &mut v;     // 合法：r1、r2 不再使用（NLL 非词法生命周期）\nrm.push(4);\n```\n**NLL（Non-Lexical Lifetimes）** 自 Rust 2018 起默认启用，编译器基于控制流图缩短借用范围，使更多代码通过检查。但核心规则不变：作用域内不能同时存在读写冲突。\n\n### 2.3 生命周期（Lifetime）\n生命周期标注是编译器验证引用有效范围的“契约”，不改变运行时行为。多数情况可自动推导（省略规则），但函数返回引用且涉及多个输入时需显式标注：\n```rust\nfn longest<'a>(x: &'a str, y: &'a str) -> &'a str {\n    if x.len() > y.len() { x } else { y }\n}\n```\n`'a` 表示返回引用的生命周期等于 `x` 和 `y` 中较短的那个。调用处编译器据此检查：\n```rust\nlet s1 = String::from(\"long\");\nlet result;\n{\n    let s2 = String::from(\"short\");\n    result = longest(&s1, &s2); // result 生命周期为 s2 的作用域\n}\n// println!(\"{result}\"); // 错误：s2 已释放\n```\n**省略规则**：每个输入引用获得独立生命周期，若只有一个输入引用则输出生命周期与之相同；方法中 `&self`/`&mut self` 的生命周期赋给输出。\n\n---\n\n## 三、智能指针 —— 超越引用的堆管理工具箱\n\n### 3.1 `Box<T>` —— 独占堆分配\n用于递归类型、转移大数据所有权、trait object：\n```rust\nenum List { Cons(i32, Box<List>), Nil }\nlet b = Box::new(5); // 堆上分配，自动释放\n```\n\n### 3.2 `Rc<T>` / `Arc<T>` —— 共享所有权\n- `Rc<T>`：单线程引用计数，非原子操作，开销低。\n- `Arc<T>`：原子引用计数，线程安全。\n\n```rust\nuse std::rc::Rc;\nlet a = Rc::new(String::from(\"shared\"));\nlet b = Rc::clone(&a);   // 计数 +1，不复制数据\nlet c = Rc::clone(&a);\nassert_eq!(Rc::strong_count(&a), 3);\n```\n`Rc<T>` 默认只提供不可变访问。若需可变性，可搭配 `RefCell`，或当引用计数为 1 时使用 `Rc::get_mut`。`Rc::make_mut` 提供写时克隆（clone-on-write）语义。\n\n**陷阱**：`Rc<RefCell<T>>` 可能产生循环引用导致内存泄漏，此时应使用 `Weak<T>` 打破循环。\n\n### 3.3 `RefCell<T>` —— 内部可变性\n将借用检查推迟到运行时：允许在持有不可变引用时修改内部值。违反规则（如同时两个 `borrow_mut()`）会触发 **panic**。\n```rust\nuse std::cell::RefCell;\nlet data = RefCell::new(42);\n*data.borrow_mut() += 1;   // 运行时可变借用\n```\n可用 `try_borrow()` / `try_borrow_mut()` 返回 `Result` 避免 panic。\n\n**经典组合** `Rc<RefCell<T>>`：单线程下多个所有者共享可变数据。\n\n| 智能指针 | 所有权 | 可变性 | 线程安全 | 检查时机 |\n|----------|--------|--------|----------|----------|\n| `Box<T>` | 独占 | 是 | 是 | 编译期 |\n| `Rc<T>` | 共享 | 否（需 RefCell） | 否 | 编译期 |\n| `Arc<T>` | 共享 | 否（需 Mutex） | 是 | 编译期 |\n| `RefCell<T>` | 不涉及 | 内部可变 | 否 | 运行时 |\n\n---\n\n## 四、unsafe Rust 与 FFI 边界\n\n五类 unsafe 超能力：\n1. 解引用裸指针\n2. 调用 unsafe 函数（含 FFI）\n3. 访问/修改可变静态变量\n4. 实现 unsafe trait\n5. 访问 union 字段\n\n```rust\nlet mut num = 5;\nlet r1 = &num as *const i32;\nlet r2 = &mut num as *mut i32;\nunsafe {\n    *r2 = 10;\n    println!(\"{}\", *r1);\n}\n```\n**FFI 示例**（调用 C 标准库）：\n```rust\nextern \"C\" { fn strlen(s: *const std::ffi::c_char) -> usize; }\nfn main() {\n    let s = std::ffi::CString::new(\"hello\").unwrap();\n    let len = unsafe { strlen(s.as_ptr()) };\n    println!(\"{len}\");\n}\n```\n**陷阱**：`CString` 拥有所有权，`as_ptr()` 返回的指针在 `CString` 被 drop 后失效，unsafe 块必须保证指针有效期内使用。暴露 Rust 函数给 C：\n```rust\n#[no_mangle]\npub extern \"C\" fn add(a: i32, b: i32) -> i32 { a + b }\n```\n**最佳实践**：将 unsafe 代码封装在安全抽象后，如 `Vec` 内部用 unsafe 操作裸指针，对外提供安全 API。\n\n---\n\n## 五、Freestanding Rust Binary —— 脱离标准库\n\n### 5.1 最小 no_std 程序\n```rust\n#![no_std]\n#![no_main]\n\nuse core::panic::PanicInfo;\n\n#[panic_handler]\nfn panic(_info: &PanicInfo) -> ! {\n    loop {}   // 裸机下最简单的处理\n}\n\n#[no_mangle]\npub extern \"C\" fn _start() -> ! {\n    // 内核入口\n    loop {}\n}\n```\n- `#![no_std]` 不链接 `std`，但可使用 `core`（提供 `Option`、`Result` 等）。\n- `#[panic_handler]` 必须定义，稳定版可用。\n- 若使用稳定版，需在 `Cargo.toml` 添加 `[profile.dev] panic = \"abort\"` 和 `[profile.release] panic = \"abort\"` 以避免需要 `eh_personality` 语言项。\n- `_start` 为 ELF 入口，`extern \"C\"` 确保 C ABI，`#[no_mangle]` 禁止名称修饰。\n\n### 5.2 编译与链接\n安装裸机目标：\n```bash\nrustup target add x86_64-unknown-none\ncargo build --target x86_64-unknown-none\n```\n自定义链接脚本（`linker.ld`）指定内存布局，在 `.cargo/config.toml` 中配置：\n```toml\n[target.'cfg(target_os = \"none\")']\nrustflags = [\"-C\", \"link-arg=-Tlinker.ld\"]\n```\n在用户态模拟 OS 时，可继续使用宿主目标（如 `x86_64-unknown-linux-gnu`）但禁用 `std`，便于用 GDB 调试。\n\n### 5.3 扩展：实现简易 println!\n通过 `core::fmt::Write` 可构建不依赖 `std` 的格式化输出。例如向 VGA 文本缓冲区写入：\n```rust\nuse core::fmt::Write;\nstruct VgaWriter { offset: usize }\nimpl Write for VgaWriter {\n    fn write_str(&mut self, s: &str) -> core::fmt::Result {\n        // 将 s 逐字节写入 0xb8000 + offset\n        Ok(())\n    }\n}\n// 宏封装\nmacro_rules! println {\n    ($($arg:tt)*) => { writeln!(VgaWriter { offset: 0 }, $($arg)*).unwrap(); }\n}\n```\n这展示了脱离标准库后仍能利用 Rust 强大的 trait 系统构建安全抽象。\n\n---\n\n**延伸阅读**：The Rust Book 第 4-10 章；Philipp Oppermann 的 *Writing an OS in Rust* 系列；Rust Nomicon 的 unsafe 与 FFI 章节。",
      "references": [],
      "quiz": [
        {
          "id": "q1",
          "type": "choice",
          "question": "In Rust, what happens when a `String` is assigned to another variable (e.g., `let s2 = s1;`)?",
          "options": [
            "A deep copy of the string data is created, and both variables are independent.",
            "The ownership is moved to `s2`; `s1` becomes invalid and cannot be used.",
            "Both `s1` and `s2` point to the same heap data, and the reference count is incremented.",
            "The compiler automatically clones `s1` because `String` implements the `Copy` trait."
          ],
          "answer": "B",
          "explanation": "`String` does not implement the `Copy` trait. Assignment moves ownership, leaving the original variable uninitialized. Using `s1` afterwards results in a compile error."
        },
        {
          "id": "q2",
          "type": "choice",
          "question": "Which of the following is true about Rust's borrowing rules with Non‑Lexical Lifetimes (NLL)?",
          "options": [
            "You can have multiple `&mut T` references to the same data as long as they are in different scopes.",
            "A `&mut T` reference can coexist with `&T` references if the immutable references are no longer used after the mutable borrow occurs.",
            "NLL allows a value to be moved while it is still borrowed, as long as the borrow is not used afterwards.",
            "The borrowing rules are relaxed at runtime; violations cause a panic instead of a compile error."
          ],
          "answer": "B",
          "explanation": "NLL shortens borrow scopes based on actual usage. If all immutable references are no longer live at the point where a mutable borrow is taken, the code compiles. However, the fundamental rule still forbids simultaneous active read and write borrows."
        },
        {
          "id": "q3",
          "type": "choice",
          "question": "Which standard library type provides *interior mutability* by enforcing borrowing rules at runtime instead of compile time?",
          "options": [
            "`Box<T>`",
            "`Rc<T>`",
            "`RefCell<T>`",
            "`Arc<T>`"
          ],
          "answer": "C",
          "explanation": "`RefCell<T>` allows mutation of its inner value even when the `RefCell` itself is immutable. It tracks borrows dynamically and will panic if a mutable borrow conflicts with another borrow. `Box`, `Rc`, and `Arc` do not provide interior mutability on their own."
        },
        {
          "id": "q4",
          "type": "choice",
          "question": "When building a freestanding Rust binary (`#![no_std]`), which of the following is **mandatory**?",
          "options": [
            "Defining a `#[panic_handler]` function.",
            "Using the nightly compiler.",
            "Implementing the `eh_personality` language item.",
            "Linking against the C standard library."
          ],
          "answer": "A",
          "explanation": "A `#[panic_handler]` is required for any `#![no_std]` binary to define what happens on panic. Nightly is not required (stable supports `#[panic_handler]` and `panic = \"abort\"`). `eh_personality` is not needed when panics abort. The C standard library is not required; you can target bare metal."
        }
      ],
      "exercise": {
        "id": "ex-1",
        "language": "rust",
        "entrypoint": "longest_word",
        "description": "Implement a function `longest_word` that takes a string slice and returns the longest word (contiguous sequence of non‑whitespace characters) as a string slice. If multiple words share the maximum length, return the first one. If the input is empty or contains only whitespace, return an empty string slice. The function must not allocate any new `String`; it must return a slice into the original string. Use proper lifetime annotations.",
        "starterCode": "/// Returns the longest word (contiguous non‑whitespace characters) in `s`.\n///\n/// If multiple words have the same maximum length, the first one is returned.\n/// If the input is empty or contains only whitespace, an empty string slice is returned.\n///\n/// # Examples\n///\n/// /// let result = longest_word(\"hello world\");\n/// assert_eq!(result, \"hello\");\n///\n/// let result = longest_word(\"a bb ccc\");\n/// assert_eq!(result, \"ccc\");\n///\n/// let result = longest_word(\"   \");\n/// assert_eq!(result, \"\");\n/// ///\n/// # Lifetimes\n/// The returned reference is tied to the input `s`; no new allocation is performed.\npub fn longest_word<'a>(s: &'a str) -> &'a str {\n    // Step 1: Split the string by whitespace to obtain an iterator over word slices.\n    //         Use `s.split_whitespace()` which yields `&str` slices.\n    let mut longest: &str = \"\";\n\n    // Step 2: Iterate over each word slice.\n    //         For each word, compare its byte length with the current longest.\n    for word in s.split_whitespace() {\n        // Step 3: If the current word is longer, update `longest`.\n        if word.len() > longest.len() {\n            longest = word;\n        }\n    }\n\n    // Step 4: Return the longest word slice (or \"\" if no words were found).\n    longest\n}",
        "testCode": "mod solution;\n\nfn main() {\n    let tests = vec![\n        (\"test 1\", \"hello world\", \"hello\"),\n        (\"test 2\", \"a bb ccc\", \"ccc\"),\n        (\"test 3\", \"   \", \"\"),\n        (\"test 4\", \"\", \"\"),\n        (\"test 5\", \"one two three\", \"three\"),\n        (\"test 6\", \"equal length words hi\", \"equal\"),\n    ];\n    for (name, input, expected) in tests {\n        let result = solution::longest_word(input);\n        let passed = result == expected;\n        let message = if passed {\n            \"\".to_string()\n        } else {\n            format!(\"expected '{}', got '{}'\", expected, result)\n        };\n        println!(\"{{\\\"name\\\": \\\"{}\\\", \\\"passed\\\": {}, \\\"message\\\": \\\"{}\\\", \\\"expected\\\": \\\"{}\\\", \\\"actual\\\": \\\"{}\\\"}}\",\n            name, passed, message, expected, result);\n    }\n}\n",
        "assertionMode": "return",
        "testCases": [
          {
            "name": "test 1",
            "input": [
              "hello world"
            ],
            "expected": "hello"
          },
          {
            "name": "test 2",
            "input": [
              "a bb ccc"
            ],
            "expected": "ccc"
          },
          {
            "name": "test 3",
            "input": [
              "   "
            ],
            "expected": ""
          },
          {
            "name": "test 4",
            "input": [
              ""
            ],
            "expected": ""
          },
          {
            "name": "test 5",
            "input": [
              "one two three"
            ],
            "expected": "three"
          },
          {
            "name": "test 6",
            "input": [
              "equal length words hi"
            ],
            "expected": "equal"
          }
        ],
        "hints": [
          "Use `s.split_whitespace()` to obtain an iterator over non‑empty slices.",
          "The returned reference must live as long as the input; annotate the function with `<'a>` and tie the output lifetime to the input.",
          "Keep track of the longest slice seen so far by comparing `.len()` values."
        ]
      },
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    },
    {
      "id": "os-arch-memory",
      "type": "unit",
      "title": "内存管理模块：虚拟内存与物理页分配器",
      "description": "实现用户态模拟操作系统的内存管理子系统。从物理内存池（基于 Vec<u8> 模拟）出发，构建页式内存分配器（buddy system 或简易页表），实现虚拟地址到物理地址的映射模拟。参考 nikofil 的 Rust-OS Kernel buddy allocator 系列及 os.phil-opp.com 的 Paging 章节。",
      "prerequisites": [
        "rust-sys-foundations"
      ],
      "objectives": [
        "理解虚拟内存、物理内存、页表的基本概念",
        "实现一个基于 buddy system 的物理页帧分配器",
        "构建简易页表结构并实现地址映射（map/unmap）",
        "实现内核堆分配器（基于分配的物理页）",
        "编写单元测试验证内存分配与回收的正确性"
      ],
      "references": [],
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    },
    {
      "id": "process-scheduler",
      "type": "unit",
      "title": "进程调度模块：任务抽象与轮转调度器",
      "description": "设计并实现进程控制块（PCB）、进程状态机以及一个简单的轮转（round-robin）调度器。使用 Rust 的协程或状态机模式模拟上下文切换，参考 nikofil 的 Rust-OS Kernel task scheduler 及 Stephen Marz 的 RISC-V OS 进程管理章节。",
      "prerequisites": [
        "os-arch-memory"
      ],
      "objectives": [
        "设计 Process 结构体（PID、状态、上下文、页表引用）",
        "实现进程状态机：Ready、Running、Blocked、Terminated",
        "实现轮转调度器（Round-Robin Scheduler）",
        "模拟上下文切换（保存/恢复寄存器状态）",
        "实现简单的进程创建与销毁 API"
      ],
      "references": [],
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    },
    {
      "id": "simple-filesystem",
      "type": "unit",
      "title": "简陋文件系统模块：类 FAT 或 inode 文件系统",
      "description": "在模拟磁盘（一个二进制文件或内存块）上实现一个极简文件系统。支持文件的创建、读写、删除以及基本的目录结构。参考 FAT32 简化设计或 Unix inode 模型，借鉴 simple-fatfs crate 和 CS3210 FAT32 lab 的设计思路。",
      "prerequisites": [
        "os-arch-memory"
      ],
      "objectives": [
        "理解文件系统基本概念：超级块、inode/FAT表、数据块",
        "设计磁盘布局并在内存中模拟块设备",
        "实现文件创建、打开、读取、写入、关闭操作",
        "实现简易目录结构（支持 mkdir、ls、cd）",
        "将文件系统与内存管理模块对接（文件缓存使用内核页）"
      ],
      "references": [],
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    },
    {
      "id": "kernel-integration-project",
      "type": "project",
      "title": "综合项目：完整玩具内核集成与 Shell 交互",
      "description": "将内存管理、进程调度和文件系统三大模块整合为一个完整的用户态模拟操作系统。实现系统调用接口层，构建一个交互式内核 Shell，支持 ps、run、ls、cat、mkdir 等命令。这是整个学习路径的核心项目，类似 CS61A 的 Ants 或 Scheme 项目规模，要求各模块协同工作并通过集成测试。",
      "prerequisites": [
        "os-arch-memory",
        "process-scheduler",
        "simple-filesystem"
      ],
      "objectives": [
        "设计并实现系统调用接口（sys_read、sys_write、sys_fork、sys_exec、sys_open 等）",
        "将三大子系统通过系统调用层解耦并集成",
        "实现一个交互式内核 Shell（命令行解析与执行）",
        "支持运行多个用户态任务并观察调度行为",
        "编写集成测试，验证端到端场景（如：加载程序→调度执行→读写文件）"
      ],
      "references": [],
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    },
    {
      "id": "polish-extend",
      "type": "unit",
      "title": "打磨与扩展：测试、文档与进阶方向",
      "description": "对完成的玩具内核进行全面测试、性能优化和文档编写。探索进阶方向如：实现写时复制（CoW）fork、多级反馈队列调度器、或简单的 ELF 加载器。参考 Redox OS 和 blog_os 的进阶内容，为后续深入学习打下基础。",
      "prerequisites": [
        "kernel-integration-project"
      ],
      "objectives": [
        "编写完整的单元测试与集成测试套件",
        "使用 Rustdoc 为内核各模块编写文档",
        "分析并优化关键路径性能（如上下文切换开销）",
        "了解进阶 OS 主题：CoW fork、多级调度、ELF 加载",
        "整理项目并产出可展示的 GitHub 仓库与 README"
      ],
      "references": [],
      "passCriteria": {
        "quizMinScore": 1,
        "exerciseMustPass": true
      }
    }
  ],
  "currentIndex": 0,
  "createdAt": "2026-06-15T03:49:38.281Z",
  "updatedAt": "2026-06-15T03:49:38.281Z"
}
