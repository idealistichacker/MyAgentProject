# Goroutine 与 Channel：Go 并发的核心模型

深入 Go 并发编程的两大基石：goroutine（轻量级协程）与 channel（CSP 通信模型）。学习使用 go 关键字启动并发任务，通过无缓冲/有缓冲 channel 在 goroutine 间传递数据，掌握 select 多路复用、range 遍历 channel、单向 channel 等关键机制。

*（注：当前学习资料为基础预备版本，您可以稍后尝试重新生成以获得大模型提炼的完整内容）*